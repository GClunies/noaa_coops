## Summary

<!-- What changed and why. 1-3 sentences. -->

## Changes

<!-- Bulleted list of notable changes. -->

## Test plan

- [ ] `uv run pytest` passes
- [ ] `uv run ruff check .` / `uv run ruff format --check .` clean
- [ ] `uv run mypy noaa_coops` clean
- [ ] New behavior has test coverage
- [ ] PR title uses Conventional Commits and states the user-visible change
- [ ] Breaking changes use `!` in the title and a `BREAKING CHANGE:` paragraph with migration instructions
- [ ] Release PR: version bump and generated changelog match the public API changes

## Notes

<!-- Anything reviewers should know: breaking changes, follow-ups, deferred items. -->
